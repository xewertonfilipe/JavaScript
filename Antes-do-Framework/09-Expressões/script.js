const groupA = 100;
const groupB = 300;
const winner = groupA > groupB ? "Grupo A Venceu" : "Grupo B Venceu";

const number = [2, 3, 4, 5, 6];
const total = number.filter(numero => numero > 4);

const active = true;
const button = active && "Botão está ativo";

const groupWinner = groupA > 90 && "Grupo A Venceu";

console.log(groupWinner);

